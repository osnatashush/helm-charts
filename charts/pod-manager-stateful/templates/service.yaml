apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-manager-stateful.fullname" . }}
  labels:
    {{- include "pod-manager-stateful.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "pod-manager-stateful.selectorLabels" . | nindent 4 }}
